import { Nav } from "./Nav";

export const Header = () => {
    return (
        <header>
            <img src="/Logo.svg" alt="Little Lemon Logo"></img>
            <Nav />
        </header>
    );
}